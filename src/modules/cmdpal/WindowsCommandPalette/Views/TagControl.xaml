<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WindowsCommandPalette.Views.TagControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsCommandPalette.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Width="128"
    Height="32">

    <Border BorderBrush="{x:Bind ViewModel.BorderBrush, Mode=OneWay}"
            BorderThickness="1"
            CornerRadius="4"
            Background="{x:Bind ViewModel.BackgroundBrush, Mode=OneWay}"
            Padding="4, 2, 4, 2"
            VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
            <ContentControl Width="12"
                            Height="12"
                            Margin="0, 0, 4, 0"
                            Visibility="{x:Bind ViewModel.HasIcon, Mode=OneWay}"
                            Content="{x:Bind ViewModel.IcoElement, Mode=OneWay}" />
            <TextBlock VerticalAlignment="Center" Foreground="{x:Bind ViewModel.TextBrush, Mode=OneWay}" FontSize="10" Text="{x:Bind ViewModel.Text, Mode=OneWay}"/>
        </StackPanel>
    </Border>
</UserControl>
